pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Running build automation'
                mvn clean package
                
            }
        }
    }
}
